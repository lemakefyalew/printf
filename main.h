  int print_hex_base(va_list arg, char _case);
  int print_hex(va_list arg);
  int print_HEX(va_list arg);
  int printva_list arg);
  void print_binary(unsigned int n, unsigned int *printed);

  /**
   *  * struct specifiersStruct - specifier definition
   *   * @specifiers: types
   *    * @{
                char *specifiers;
                        int (*printer)(va_list);
} specifiersStruct;

#endif
